# dependencies
node_modules/

# build + generated artifacts (regenerated by `npm run build` / `npm run catalog`)
dist/
public/
catalog.json

# packaged output + zips
package/
*.zip

# runtime state (per-deploy; never commit)
.sophia-data/
**/.sophia-data/

# test scratch
demo/out/

# misc
*.log
.DS_Store
